"""native.scripts.deps -- manifest-driven third-party dependency acquisition.

Round-1 scope (D1/D2, owned by M1): `manifest`, `render`. `fetch`/`run`/
`assertions`/`patches`/`build_deps.py` belong to D3/D4/D10 (M3) and are added
in later rounds by other owners.
"""
