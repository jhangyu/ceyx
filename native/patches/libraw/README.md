# Project-authored LibRaw patches

These diffs are rooted at the **LibRaw** tree
(`native/third_party/libraw/`), not at RawSpeed3. They are applied
by `scripts/fetch_libraw_dist.sh` after LibRaw's own `RawSpeed3/patches/*.patch`
set and before the `.git` directories are stripped.

Numbering continues from LibRaw's own set (01-05) so a patch number is globally
unique in this project's provenance record.

| Patch | Target file | Purpose |
|---|---|---|
| 06.fuji-tryrawspeed3.patch | `src/utils/decoder_info.cpp` | Sets `LIBRAW_DECODER_TRYRAWSPEED3` on `fuji_compressed_load_raw` and `fuji_14bit_load_raw` so RAF files are offered to RawSpeed3 first (Phase 19 W1). |
| 08.x3f-parallel-lut.patch | `src/x3f/x3f_utils_patched.cpp` | Foveon X3F TRUE-engine acceleration: 3-plane std::thread parallel decode + 256-entry packed branchless Huffman LUT fast path driven by a register-resident 64-bit bit window, with verbatim tree-walk fallback (RAW decode accel round 2026-08-27; round-2 R4 window/branchless rework 2026-08-28 — a 16-bit two-symbol pair LUT was measured and rejected, floors 162/169 ms vs 146/146 ms, see `native/scripts/tmp/foveon_impl_artifacts/`). |
| 09.fuji-stdthread-parallel.patch | `src/decoders/fuji_compressed.cpp` | Fuji compressed RAF (lossless + lossy) block-parallel decode via bounded std::thread pool, plus a real mutex for the datastream sites upstream leaves unguarded or under `omp critical` (RAW decode accel round, 2026-08-27). Round 2 (2026-08-28, contract R1 form A): the std::thread pool is now the single fuji path on every platform — the OpenMP branch is removed from this file (its per-byte `omp critical` thread-id lookup cost −11…−15% wall, measured) — and the pool gains a thread-count cap (default 16, `CEYX_FUJI_DECODE_THREADS` override). |
| 10.fuji-qtable-cache.patch | `src/decoders/fuji_compressed.cpp` | Round-2 contract R2: per-strip memoisation of `init_main_qtable`/`setup_qlut` output keyed by `q_base` (pure function of q_base for a fixed file; 9.9% measured profile share). `init_main_grads` still runs on every q_base transition. Applies on top of patch 09. |
| 12.normalize-model-orig-race.patch | `src/metadata/normalize_model.cpp` | R4 item 2 (2026-09-05): fixes a shared-static data race at `GetNormalizedModel()` line 406 affecting every non-Sony brand (Fujifilm/Kodak/Leaf/Konica-Minolta/Nikon/Olympus/Panasonic/PhaseOne/Samsung-Pentax/Samsung) — `static const char *orig;` was one process-global pointer racing across concurrent decodes of different camera models. Changed to a per-call automatic local (`const char *orig = "";`); every alias table's `'@'`-prefixed-first-element convention (mechanically checked, `native/scripts/check_alias_table_convention.py`) guarantees it is always written before read within one call. Full rationale and red→green evidence: `native/third_party/libraw/PROVENANCE.md` and `docs/logs/2026-09-05/race2/`. |
| 13.neon-validation-sweep.patch | `src/decoders/generic.cpp` | T-V1 (2026-09-19, CPU front-end levers campaign): NEON `uint16x8` vectorisation of `unpacked_load_raw()`'s per-pixel validation sweep (measured 10.54 ms / 34.1% of a 30.9 ms decode, single-threaded scalar). Behaviour-preserving -- `derror()` fires for exactly the same `(row, col)` pairs and exactly the same number of times as the scalar loop, since `data_error` (`utils_libraw.cpp:187`) is an unconditional per-call counter, not a latch. `load_flags != 0` (a destructive in-place shift-and-store) falls back to the verbatim scalar loop, the spec's recommended default. The row margin test is hoisted and the column loop is restricted to the visible span; within that span 8 lanes are OR-reduced against the overflow threshold, and only a chunk whose reduction is non-zero re-walks scalar to preserve the exact per-pixel `derror()` call count. `bits >= 16` (no 16-bit sample can overflow) skips the column work entirely. Guarded `#if defined(__ARM_NEON)`; the scalar path (Windows/Linux x86-64) is untouched. Design: `docs/logs/2026-09-19/spec-cpu-levers.md` §4.3. |
| 14.nikon-he-decoder-import.patch | `src/decoders/nikon_he/*` (32 new files), `src/decoders/nikon_he_decoder.cpp` | Add-files-only import of the Nikon High Efficiency (HE) / High Efficiency* (HE*) decoder, byte-verbatim from `zidage/LibRaw@fca3ee4db0b51f3787534fc370891e93ceb52f59` (branch `feature/nikon_he_support`), which extends Dmitri Sotnikov's clean-room HE decoder from LibRaw PR #826 (`yogthos/LibRaw@499bfd4cbf802c2fb5eac4c9a6f53bbeb776cad1`) with HE* support. Compiled into the `raw` target automatically by the LibRaw-cmake `file(GLOB_RECURSE ... src/*.cpp)` at `native/third_party/libraw-cmake/CMakeLists.txt:464` — no CMake edit. Inert until patch 15 removes the throwing stub. Provenance, authorship and the clean-room caveat: `native/third_party/libraw/PROVENANCE.md`, section "Nikon HE/HE* decoder import". Known upstream defect (compile-time, desktop OpenMP ON): `src/decoders/nikon_he/nikon_he_decode.cpp` contains two syntactic `#pragma omp ordered` blocks inside one `#pragma omp for ordered` loop (lines ~128, ~154) which Apple clang's OpenMP implementation rejects at compile time even though only one executes per iteration at runtime; 15 of 16 new translation units compile, this one does not. Left byte-verbatim per this patch's own scope (add-files only, zero behavioural edits); the fix is scoped to patch 15 (Task 2) as a minimal behaviour-preserving restructure. |
| 15.nikon-he-dispatch.patch | `src/decoders/decoders_libraw_dcrdefs.cpp`, `src/metadata/tiff.cpp`, `src/tables/cameralist.cpp`, `src/decoders/nikon_he/nikon_he_decode.cpp` | Activates the HE/HE* decoder imported by patch 14: removes the throwing `LibRaw::nikon_he_load_raw()` stub (the real definition lives in `src/decoders/nikon_he_decoder.cpp`), replaces the four-model allowlist on the JPEG-XS SOC marker branch with the marker sniff alone plus a short-read guard (`got == sizeof(buf)`), and drops the now-false "HE/HE* formats are not supported yet" suffix from four Nikon camera-list strings. First three edits transcribed from `zidage/LibRaw@fca3ee4db0b51f3787534fc370891e93ceb52f59`; the unrelated Sony A7 V camera-list line in the same upstream hunk was deliberately not imported. The fourth edit is this project's own minimal fix for an upstream defect: `nikon_he_decode.cpp` had two syntactic `#pragma omp ordered` blocks in mutually exclusive branches of one `#pragma omp for ordered` loop, which OpenMP forbids and Apple clang rejects statically (15/16 translation units compiled; artifact `native/scripts/tmp/nikon_he_build/task1_compile5.txt`). The early-out is now a local flag and a single ordered region branches on it — same ordered sequence, one entry per iteration, no pragma removed and no per-translation-unit OpenMP opt-out (that would be a platform fork). |
| 16.nikon-he-refactor.patch | `src/decoders/nikon_he/*`, `src/decoders/nikon_he_decoder.cpp` | Behaviour-preserving cleanup of the decoder imported byte-verbatim by patch 14: spelled-out identifiers (format terms GCLI/GTLI/IDWT/Bp/Br kept as domain vocabulary), dead-code removal proven by call-site grep, and comment corrections. No algorithmic, arithmetic-ordering, OpenMP, or SIMD-strategy change. Behaviour contract: identical `fnv1a` decode hashes for the HE and HE* corpus samples before and after, checked by `native/scripts/tmp/nikon_he_refactor/check_behaviour_preserved.py` (positive control performed: an injected one-line change made the check fail before the real work started). Patch 14 stays verbatim so the upstream provenance chain remains auditable. |
| 17.nikon-he-cleanup.patch | `src/decoders/nikon_he/nikon_he_bayer.cpp`, `nikon_he_bayer.h`, `nikon_he_coefficient_decode.cpp`, `nikon_he_decode.cpp`, `nikon_he_idwt_horizontal.cpp`, `nikon_he_simd.h`, `nikon_he_tile.h` | Behaviour-preserving cleanup, refactor round 3 — the user-approved parking-lot items left over from patch 16. Patch 16 stays frozen; this is a separate patch so each round stays independently auditable. Four kinds of change, no other: (a) an unreachable-guard correction and dead-code removal proven by call-site grep (`kVerLiftLoopsPerTile`, `openmp_max_threads()`); (b) one duplicate computation in `nikon_he_bayer.cpp` removed after verifying both sites used byte-identical expressions over operands not mutated between them, so the second is a direct assignment rather than a recomputation; (c) the two ~40-line near-identical halves of `idwt_horizontal_pass()` extracted into one `idwt_horizontal_pass_levels()` helper — the code is moved verbatim and only the three values that actually differed are parameterised (lift level count 5 vs 1, the per-level group-count source, and the `SubbandConfig` index 12 vs 23); (d) comment/naming nits including removal of two `(void)` casts on parameters that are in fact used two lines above. No algorithmic, arithmetic-ordering, OpenMP, or SIMD-strategy change. Behaviour contract: byte-identical `fnv1a` decode hashes for both corpus samples — `nikon_z8_he.nef` `0xf2b68e94a4bfd717` and `nikon_z8_he_star.nef` `0xc38f6dceb167be20`, both `w=8280 h=5520 rc=0`. Each contributor's edits were gated separately against a clean post-patch-16 base so any regression would be attributable, and a positive control (an injected one-line change inside `idwt53_inverse_one_level`) was shown to turn both hashes red before the greens were accepted; evidence under `native/scripts/tmp/nikon_he_cleanup/`. |

Patch 07 (`07.fuji-rotated-gate.patch`, conditional, would exempt `filters == 9`
X-Trans from the `!IO.fuji_width` clause of the RawSpeed3 gate in
`src/decoders/unpack.cpp`) was **not created**: Task 1's measurement
(`native/scripts/tmp/p19/t1_eligibility.txt`) showed
`fuji_width=0` for both corpus RAF samples both before and after patch 06, so
the `!IO.fuji_width` clause never blocks these files and the extra patch is
unnecessary per the decision rule in `t1_prereg.md`.

Every patch here must have its SHA-256 recorded in
`third_party/libraw/PROVENANCE.md` under "Project-authored LibRaw patches".
`scripts/verify_raw_provenance.py` fails if a patch file has no recorded digest,
or if its added lines are not present in the vendored tree.

**The vendored tree is never edited except through a patch in this directory.**
